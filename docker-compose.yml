version: '3.8'

services:
  clinic-system:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - JWT_SECRET=your-secure-jwt-secret-here
    restart: unless-stopped
